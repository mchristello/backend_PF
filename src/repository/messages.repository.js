import MessageDTO from '../dao/DTO/messages.dto.js';

export default class MessageRepository {

    constructor(dao) {
        this.dao = dao;
    }

    get = () => {

    }

    send = () => {
        
    }
}